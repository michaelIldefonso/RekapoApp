name: Build Android

on:
  workflow_dispatch:  # Manual trigger
  push:
    tags:
      - 'preview*'  # Only trigger on preview tags

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # cache: 'npm'  # Disabled due to GitHub cache issues

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Build Android APK
        run: eas build --platform android --profile preview --non-interactive --local
        env:
          GOOGLE_WEB_CLIENT_ID: ${{ secrets.GOOGLE_WEB_CLIENT_ID }}
          BACKEND_API_URL: ${{ secrets.BACKEND_API_URL }}
          FIREBASE_CONFIG_URL: ${{ secrets.FIREBASE_CONFIG_URL }}

      - name: Rename APK
        run: mv build-*.apk RekapoApp.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: preview-apk
          path: RekapoApp.apk
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Preview Build - ${{ github.ref_name }}
          body: |
            Preview build for thesis survey
            Built from commit: ${{ github.sha }}
          files: RekapoApp.apk
          draft: false
          prerelease: true
